# 开源范围与内容权利

## AGPL 覆盖范围

除文件另有明确声明外，本仓库中的原创源代码、构建脚本和部署配置按
`AGPL-3.0-only` 发布。完整条款见根目录 [LICENSE](../LICENSE)。

## 不自动适用 AGPL 的内容

以下内容不会仅因由本软件处理或生成而自动变为 GPL 内容：

- 用户上传的商品图、人物图、品牌素材和业务数据；
- 外部模型、LoRA、ControlNet、ComfyUI 工作流及其权重；
- 模型 Provider 返回的生成图片；
- AI_image2 的项目名称、官方 Logo 和其他品牌标识；
- 明确标注其他许可证的第三方组件、字体和数据集。

上述内容分别受权利人、模型服务商、平台规则和适用法律约束。软件许可证不构成对输出
图片可商用、人物已授权或电商平台会接受该图片的保证。

## 发布和分发

修改版通过网络向用户提供交互服务时，应按照 AGPLv3 第 13 条向这些用户显著提供该运行
版本对应源码的访问方式。分发修改版、二进制包或容器镜像时，还应同时遵守 AGPLv3 的
对应源码义务和镜像中每个第三方组件的许可证要求。发布版本应对应到可复现的 Git 标签，
并保留构建脚本、锁文件、版权声明和第三方通知。
