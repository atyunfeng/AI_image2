export type ProductReference = { id: string; asset_id: string; view: "front" | "side" | "back" | "detail" | "logo"; sha256: string; mime_type: string; size_bytes: number };
export type Product = { id: string; sku: string; name: string; category: "apparel" | "shoes" | "hats" | "other"; references?: ProductReference[] };
export type ModelConfiguration = { id: string; name: string; provider: string; model_id: string; base_url: string | null; capabilities: string[]; has_key: boolean; key_suffix: string | null; is_enabled: boolean };
export type Batch = { id: string; production_plan_id?: string | null; production_plan_item_id?: string | null; product_id: string; model_configuration_id: string; requested_view: string; mode: string; status: string; prompt: string; width: number; height: number; output_asset_id?: string | null; provider_request_id?: string | null; estimated_cost_minor?: number; error_classification?: string | null };
export type TemplatePack = { id: string; version_id: string; slug: string; name: string; kind: "platform" | "category" | "brand"; version: number; status: "published"; rules: Record<string, unknown>; source: string; published_at: string };
export type ProductionPlanItem = { id: string; position: number; slot: string; label: string; requested_view: string; width: number; height: number; prompt: string; authoritative_copy?: string | null; rules: Record<string, unknown> };
export type ProductionPlan = { id: string; product_id: string; mode: string; compiler_hash: string; compiled_snapshot: { packs: Record<string, { slug: string; version: number }> }; created_at: string; items: ProductionPlanItem[] };
export type QualityCheck = { code: string; passed: boolean; blocking: boolean; expected: Record<string, unknown>; measured: Record<string, unknown>; message: string };
export type QualityRun = { id: string; batch_id: string; output_asset_id: string; passed: boolean; created_at: string; checks: QualityCheck[] };
