"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export function ModelForm(){const router=useRouter();const [message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget;const data=new FormData(form);const provider=String(data.get("provider"));const response=await fetch("/api/backend/models",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:data.get("name"),provider,model_id:data.get("model_id"),base_url:data.get("base_url")||null,api_key:data.get("api_key")||null,capabilities:["reference_to_image"]})});if(response.ok){form.reset();setMessage("模型配置已保存，密钥不会再次显示");router.refresh();}else setMessage("保存失败，请检查配置");}return <form onSubmit={submit} className="form-grid"><input className="field" name="name" placeholder="配置名称" required/><select className="field" name="provider"><option value="mock">Mock（本地验收）</option><option value="generic_http">通用 HTTP API</option></select><input className="field" name="model_id" placeholder="模型 ID" required/><input className="field" name="base_url" placeholder="API Base URL（Mock 可留空）"/><input className="field" name="api_key" type="password" placeholder="API KEY（加密保存）"/><button className="primary-button">保存模型</button>{message&&<p className="text-xs text-slate-400">{message}</p>}</form>;}
