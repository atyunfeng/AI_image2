import { ModelConfiguration } from "@/lib/types";
export function ModelList({models}:{models:ModelConfiguration[]}){if(!models.length)return <p className="empty-copy">还没有模型配置。</p>;return <div className="space-y-3">{models.map(m=><div className="row-card" key={m.id}><div className="row-card-main"><p className="font-semibold">{m.name}</p><p className="text-xs text-slate-500">{m.provider} / {m.model_id} · {m.billing_currency}</p></div><span className="row-card-meta text-sm text-slate-400">{m.has_key?`••••${m.key_suffix}`:"无需密钥"}</span><span className="status-pill">{m.is_enabled?"可用":"停用"}</span></div>)}</div>;}
