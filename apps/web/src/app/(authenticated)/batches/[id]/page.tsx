/* eslint-disable @next/next/no-img-element */
import{BatchStatus}from"@/features/batches/batch-status";import{ReviewPanel}from"@/features/review/review-panel";import{safeApiFetch}from"@/lib/api-client";import{Batch}from"@/lib/types";
export default async function BatchDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;const batch=await safeApiFetch<Batch|null>(`/batches/${id}`,null);if(!batch)return <p className="empty-copy">批次不存在或服务暂不可用。</p>;return <div className="space-y-7"><div className="flex items-end justify-between"><div><p className="eyebrow">BATCH {id.slice(0,8)}</p><h1 className="page-title">生成与审核</h1></div><BatchStatus status={batch.status}/></div><div className="grid gap-6 xl:grid-cols-[1fr_380px]"><section className="panel p-6"><div className="aspect-square overflow-hidden rounded-2xl border border-white/10 bg-[#090b12]">{batch.output_asset_id?<img src={`/api/backend/assets/${batch.output_asset_id}/content`} alt="生成结果" className="h-full w-full object-contain"/>:<div className="grid h-full place-items-center text-slate-600">模型正在处理 · 页面自动刷新</div>}</div><dl className="mt-5 grid gap-3 text-sm sm:grid-cols-2"><div><dt className="text-slate-500">视角 / 模式</dt><dd>{batch.requested_view} / {batch.mode}</dd></div><div><dt className="text-slate-500">尺寸</dt><dd>{batch.width} × {batch.height}</dd></div><div><dt className="text-slate-500">请求 ID</dt><dd className="break-all font-mono text-xs">{batch.provider_request_id??"—"}</dd></div><div><dt className="text-slate-500">成本</dt><dd>{batch.estimated_cost_minor??0} minor</dd></div></dl></section><ReviewPanel batch={batch}/></div></div>}
