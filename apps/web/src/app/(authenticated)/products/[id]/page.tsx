import{ReferenceUpload}from"@/features/products/reference-upload";import{safeApiFetch}from"@/lib/api-client";import{Product}from"@/lib/types";
export default async function ProductDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;const product=await safeApiFetch<Product|null>(`/products/${id}`,null);if(!product)return <p className="empty-copy">商品不存在或服务暂不可用。</p>;return <div className="space-y-7"><div><p className="eyebrow">{product.sku}</p><h1 className="page-title">{product.name}</h1></div><div className="grid gap-6 xl:grid-cols-[1fr_340px]"><section className="panel p-6"><h2 className="section-title mb-5">不可变参考图</h2>{product.references?.length?<div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{product.references.map(ref=><div key={ref.id} className="overflow-hidden rounded-xl border border-white/10"><img src={`/api/backend/assets/${ref.asset_id}/content`} alt={`${ref.view}参考图`} className="aspect-square w-full object-contain bg-white"/><p className="p-3 text-xs text-slate-400">{ref.view} · {ref.sha256.slice(0,10)}</p></div>)}</div>:<p className="empty-copy">至少上传一张正面图后再生成。</p>}</section><section className="panel h-fit p-6"><h2 className="section-title mb-5">新增视角</h2><ReferenceUpload productId={id}/></section></div></div>}
